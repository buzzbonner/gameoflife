name: .NET

on:
  push:
  
env:
  BRANCH_NAME: 'feature/this_is_a_test'
  # ${{ github.head_ref || github.ref_name }}

jobs:
  build:

    runs-on: windows-latest

    steps:
    - name: Setup .NET
      run: echo "BRANCH_NAME=$(echo $BRANCH_NAME | cut -c 1-2)" >> $GITHUB_ENV
    - name: Test
      run: echo $BRANCH_NAME
